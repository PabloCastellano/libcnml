language: python

python:
  - "2.7"

branches:
  only:
    - master

# command to install requirements
install:
  - python setup.py -q install

# command to run tests, e.g. python setup.py test
script:
  - python ./libcnml/tests/test_libcnml.py
